# Candidate selected from a train_phase1 validation split (test_size=0.2,
# stratified, random_state=42). The held-out eval.csv is scored only after
# this candidate is fixed.
model_type: random_forest
model_params:
  n_estimators: 10
  max_depth: 3
  min_samples_split: 2
  max_features: null
  class_weight: null
  n_jobs: -1
  random_state: 42
validation:
  test_size: 0.2
  random_state: 42
