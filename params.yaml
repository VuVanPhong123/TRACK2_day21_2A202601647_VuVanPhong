# Temporary intentionally weak RandomForest configuration for eval-gate evidence.
model_type: random_forest
feature_engineering:
  families:
    - density_alcohol
    - sulfur_alcohol
model_params:
  n_estimators: 10
  max_depth: 3
  min_samples_split: 2
  max_features: sqrt
  class_weight: null
  n_jobs: -1
  random_state: 42
validation:
  test_size: 0.2
  random_state: 42
